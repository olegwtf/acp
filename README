Для запуска из текущей директории необходимо предварительно запустить скрипт reconf, который поменяет все пути к файлам на относительные.
Для обратной замены запустите reconf повторно.

Для сборки deb пакета запустите скрипт build_deb
